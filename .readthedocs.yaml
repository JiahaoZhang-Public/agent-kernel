version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  commands:
    - pip install mkdocs-material "mkdocstrings[python]>=0.25"
    - pip install -e .
    - mkdocs build --site-dir $READTHEDOCS_OUTPUT/html
